/** @type {import('next').NextConfig} */
const nextConfig = {

    output: 'export',
    //basePath: '/front',
    eslint: {
        ignoreDuringBuilds: true,   
    },
}

module.exports = nextConfig
